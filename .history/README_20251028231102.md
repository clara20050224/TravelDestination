# 🌍 Travel Destination App

## 📖 Deskripsi Singkat
Aplikasi sederhana untuk menampilkan daftar destinasi wisata dunia.  
Pengguna dapat melihat daftar destinasi dan mengklik salah satunya untuk melihat detail informasi.

---

## ⚙️ Komponen Utama
1. **App.jsx**  
   - Komponen utama yang menyimpan state `selectedDestination` dan mengirim data ke komponen lain menggunakan props.

2. **DestinationList.jsx**  
   - Menampilkan daftar semua destinasi dalam bentuk kartu.
   - Menerima data destinasi melalui **props** dari `App.jsx`.

3. **DestinationDetail.jsx**  
   - Menampilkan detail destinasi yang dipilih.
   - Data detail dikirim melalui **props** dari `App.jsx`.

---

## 🔄 Penggunaan Props dan State
- **State** digunakan di `App.jsx`:
  ```js
  const [selectedDestination, setSelectedDestination] = useState(null);
