# 🌍 Travel Destination App

Aplikasi React sederhana untuk menjelajahi berbagai destinasi wisata dunia seperti Korea, Jepang, Swiss, New York, Jerman, Thailand, dan Belanda.

## 🧭 Deskripsi Singkat

Aplikasi ini menampilkan daftar destinasi di sisi kiri dan detail destinasi (gambar + deskripsi) di sisi kanan.  
Ketika pengguna memilih destinasi, tampilan detail akan berubah **secara dinamis**.

## ⚙️ Komponen Utama

1. **Header.jsx**  
   Menampilkan judul aplikasi di bagian atas.

2. **DestinationList.jsx**  
   Menampilkan daftar destinasi.  
   - Menggunakan `props` untuk menerima data `destinations` dari `App.jsx`.  
   - Menggunakan `props` fungsi `onSelect` untuk mengirim pilihan destinasi ke `App.jsx`.

3. **DestinationDetail.jsx**  
   Menampilkan informasi lengkap destinasi yang dipilih.  
   - Menerima data destinasi dari `App.jsx` melalui `props`.

4. **App.jsx**  
   Komponen utama yang menyimpan `state`:
   - `selectedDestination` untuk menampung destinasi yang sedang dipilih.
   - `setSelectedDestination` digunakan untuk memperbarui state secara dinamis.

## 🔄 Props & State

| Komponen | Props | State |
|-----------|--------|--------|
| `App.jsx` | – | `selectedDestination` |
| `DestinationList.jsx` | `destinations`, `onSelect` | – |
| `DestinationDetail.jsx` | `destination` | – |

## 🎨 Tampilan

Tampilan dibuat dengan **Tailwind CSS** agar modern, responsif, dan mudah digunakan.

## 🚀 Cara Menjalankan

```bash
npm install
npm run dev
